---
name: project-parser-telegram
description: Личный Telegram-парсер на Telethon — задеплоен на CZ-VPS SmartApe, работает под systemd
metadata: 
  node_type: memory
  type: project
  originSessionId: 50458a48-b328-4539-bfc9-718f17bd7eae
---

Личный парсер заявок из TG-чатов. Проект в `/Users/konstantinsabalin/Documents/Парсер/`, полный статус в `CLAUDE.md` проекта.

**Этап 2 (MVP) закрыт 2026-05-14:** TG-аккаунт `@kreohunter` залогинен (QR + 2FA), парсер слушает 13 чатов «Авто из Китая», 188 ключей + Claude Haiku фильтр, канал выдачи «Авто из Китая – лиды» (id=3811077544), бот `@avto_leads_bot`.

**Этап 5 (деплой) закрыт 2026-05-15:**
- Активный VPS: SmartApe HDD S1 #1634595, `85.137.165.185`, Host-Telecom (CZ, Hodonín), Ubuntu 24.04, 345₽/мес, действует до 2026-06-15. Root-доступ в `.env` проекта.
- Парсер в `/opt/parser/`, venv поставлен, systemd-юнит `/etc/systemd/system/parser.service` (enabled+running). Логи: `/var/log/parser.log`.
- **Хвосты в биллинге (отменить!):** старый MSK `1634364` (188.127.254.194, заблокирован РКН) + дубль CZ `1634584` (85.137.165.45, оформлен по ошибке поддержкой при миграции). Оба активны, по 345₽/мес — нужно тикет в SmartApe с возвратом на баланс.

**Ротация секретов 2026-05-15:** VPS root password сменён через панель SmartApe; Anthropic API key пересоздан (`parser-vps-2026-05-15`), залит на сервер, старый удалён. Bot token ещё не ротирован.

**Биллинг Anthropic (вести даты — см. [[feedback-фиксировать-биллинг-даты]]):**
- **Последнее пополнение баланса: 2026-05-14** (по словам пользователя, console Billing). Стартовый депозит $5.
- **Баланс иссяк 2026-06-22 ~09:57 MSK** — Haiku начал отдавать `400 credit balance is too low`, заявки молча терялись (`main.py` ловит исключение → `haiku_errors++` → `return`). Боевой ключ на VPS (`...qPIQAA`) валиден, проблема только в деньгах. Локальный ключ в `.env` проекта (`...51TgAA`) — мёртвый старый (401), в бою не используется.
- Решение пользователя 2026-06-22: баланс пока НЕ пополнять; вместо этого паузим нишу «Авто из Китая», запускаем «Контент» БЕЗ Haiku (только ключи+антиключи), флаг `"ai_filter": false` в niche.json.

**Этап 3 (тюнинг фильтра) в работе с 2026-05-15:**
- Проанализирован экспорт переписки с клиентом: 28 пересланных лидов, единственная закрытая продажа — Audi Q3 Club (@pidjakov_a → Андрей, оплатил 17.03). Audi Q3 — конвертирующая модель.
- Документ критериев: `критерии_заявок.md` + `.html` в корне проекта. Главное правило «2 из 3 признаков», политика по регионам (ТОЛЬКО Китай), дедуп окно 7 дней по `user_id`.
- Кандидаты новых чатов через Tavily — в `новые_чаты_кандидаты.html`.

**Большая итерация 2026-05-15 вечер (НЕ ЗАДЕПЛОЕНО на VPS на момент 18:30):**
- **Наблюдаемость:** добавлены счётчики `STATS` (окно за час) и `TOTAL` (с запуска сервиса). Каждый час сводка в канал выдачи: всего сообщений / прошли ключи / антиключ блокировал / Haiku одобрил-отклонил-ошибки / ошибки отправки. Формат «окно / суммарно». Константа `STATS_INTERVAL_SEC=3600` ([main.py:43](main.py#L43)).
- **Telethon-шум заглушён** через `logging.getLogger("telethon").setLevel(logging.CRITICAL)`. Ранее `PersistentTimestampOutdatedError` забивал лог.
- **Антиключи — теперь в коде** (раньше только в промпте Haiku). Новый файл `авто_китай_антиключи.txt` (44 стопа), грузится в `config.py`, проверка в `main.py` после ключей и до Haiku. Группы: правый руль/RHD, страны-источники (Япония/Корея/Германия/Дубай/ОАЭ/Европа), уже-состоявшийся пригон.
- **Свежие критерии от клиента (Владислав, 15.05 10:51):** Китай 3-5 лет, ≤160 л.с., только левый руль. Подтверждённый список 45 моделей (Audi Q2L/A3/A4L, BMW 1/X1/3, Mercedes GLA/A/GLB, VW Polo/Sagitar/Bora/Lavida/Golf/T-Cross/Tiguan L/Tharu/Passat/Magotan/Tayron, Skoda Octavia/Kamiq/Karoq/Superb, Jetta VS5/VS7, Nissan Qashqai/Sylphy/Kicks, Toyota Corolla, Honda Fit/Life/Crider/Vezel/XR-V/Civic, Chevrolet Monza, Kia K3, Hyundai Elantra/ix35, Mazda 3/CX-4/CX-30/CX-5, Subaru XV). Китайские бренды (BYD/Li Auto/Zeekr/Geely/Chery/Haval/Exeed/Hongqi/GAC/Changan и т.д.) — НЕ берёт.
- **Промпт Haiku полностью переписан** ([filter.py:9-36](filter.py#L9-L36)) под эти критерии — структура ЧТО ВОЗИТ / ДА / НЕТ, явные списки брендов и моделей.
- **Ключи +85** на модели → 332 ключа в `авто_китай_ключи.txt`.
- **Чаты +4** → 23 чата: `redLichat` (Li Auto, китайский бренд — много шума ожидаем), `tiggo8_club` (Chery, аналогично), `bmwf30russia` (BMW 3 F30 — граничный возраст), `sonata_dn8` (Hyundai Sonata DN8).

**⚠️ Деплой на VPS в следующей сессии — команды лежат в `CLAUDE.md` проекта**, секция «НЕ ЗАДЕПЛОЕНО НА VPS». Шесть файлов: `main.py`, `filter.py`, `config.py`, `авто_китай_ключи.txt`, `авто_китай_антиключи.txt`, `авто_китай_чаты.txt`. После деплоя — рестарт сервиса и проверка статуса/логов.

**Несделано:** реализация дедупликации в SQLite (схема + проверка перед отправкой); решение по кандидатам из `новые_чаты_кандидаты.html`; следующая итерация разметки после набора 20-30 находок.

**Why:** деплой делали раньше Этапа 3 (тюнинг фильтра), чтобы парсер копил находки 24/7. RU-датацентры режут MTProto — обязательно зарубежный ДЦ.

**How to apply:** управление — `ssh root@85.137.165.185`, `systemctl restart|stop|status parser`, `tail -f /var/log/parser.log`. Полный статус Этапа 3 — в `CLAUDE.md` проекта.

См. [[reference-russian-vps-telegram-block]], [[reference-smartape-accounts]].
